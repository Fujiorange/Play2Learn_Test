# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=play2learn

# MongoDB Configuration (if using MongoDB)
MONGODB_URI=mongodb://localhost:27017/play2learn

# Email Configuration
# For production (Render), use an SMTP service like:
# - Gmail: smtp.gmail.com (port 587 or 465)
# - Outlook: smtp-mail.outlook.com (port 587)
# - SendGrid: smtp.sendgrid.net (port 587)
# - Mailgun: smtp.mailgun.org (port 587)
# - AWS SES: email-smtp.region.amazonaws.com (port 587)

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM="Play2Learn <your-email@gmail.com>"

# Frontend URL
# For local development: http://localhost:3000
# For Render deployment: https://your-app-name.onrender.com
FRONTEND_URL=http://localhost:3000

# Server Configuration
PORT=5000

# JWT Secret (optional, if using JWT)
JWT_SECRET=your-secret-key-here

# NOTES:
# 1. For Gmail: Use App Password, not your regular password
#    - Enable 2FA on your Google account
#    - Generate App Password at: https://myaccount.google.com/apppasswords
#    - Use EMAIL_PORT=587 and EMAIL_SECURE=false (or port 465 with EMAIL_SECURE=true)
#
# 2. For Outlook/Hotmail:
#    - Use EMAIL_HOST=smtp-mail.outlook.com
#    - Use EMAIL_PORT=587 and EMAIL_SECURE=false
#
# 3. For production deployment on Render:
#    - Set all these environment variables in Render dashboard
#    - Never use localhost or 127.0.0.1 for EMAIL_HOST
#    - Set FRONTEND_URL to your deployed frontend URL
