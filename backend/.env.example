# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=play2learn

# MongoDB Configuration (REQUIRED for production)
# For local development:
MONGODB_URI=mongodb://localhost:27017/play2learn
# For Render/production (MongoDB Atlas):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/play2learn?retryWrites=true&w=majority

# Email Configuration
# IMPORTANT: These settings are REQUIRED for sending welcome emails and credentials
# See EMAIL_SETUP_GUIDE.md for detailed setup instructions

# === Gmail Configuration (Recommended for Testing) ===
# 1. Enable 2FA: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character App Password (not your regular password)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password
EMAIL_FROM="Play2Learn <your-email@gmail.com>"

# === SendGrid Configuration (Recommended for Production) ===
# 1. Sign up: https://sendgrid.com
# 2. Create API key in Settings → API Keys
# 3. Verify sender in Settings → Sender Authentication
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASSWORD=SG.your-sendgrid-api-key
# EMAIL_FROM="Play2Learn <verified-sender@yourdomain.com>"

# === Outlook/Hotmail Configuration ===
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@outlook.com
# EMAIL_PASSWORD=your-outlook-password
# EMAIL_FROM="Play2Learn <your-email@outlook.com>"

# === Mailgun Configuration ===
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=postmaster@your-domain.mailgun.org
# EMAIL_PASSWORD=your-mailgun-smtp-password
# EMAIL_FROM="Play2Learn <noreply@your-domain.com>"

# Frontend URL
# For local development: http://localhost:3000
# For Render deployment: https://your-app-name.onrender.com
FRONTEND_URL=http://localhost:3000

# Server Configuration
PORT=5000

# JWT Secret (REQUIRED for production)
# IMPORTANT: Generate a strong random string for production!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here

# User Deletion PIN
# Set this to a secure PIN for user deletion confirmation
# Use a strong, unpredictable value in production (e.g., a random 8+ character code)
USER_DELETION_PIN=7h9p2k5m

# Node Environment (set to 'production' on Render)
NODE_ENV=development

# NOTES:
# 1. EMAIL SETUP:
#    - See EMAIL_SETUP_GUIDE.md for complete step-by-step instructions
#    - For Gmail: Must use App Password (not regular password) - requires 2FA
#    - For SendGrid: EMAIL_USER must be exactly "apikey"
#    - Test locally with: node backend/test-email.js
#
# 2. For production deployment on Render:
#    - Set all environment variables in Render dashboard (not in code)
#    - Never use localhost or 127.0.0.1 for EMAIL_HOST
#    - Set FRONTEND_URL to your deployed app URL
#    - Set NODE_ENV=production
#    - Generate a strong JWT_SECRET (see command above)
#
# 3. See these guides for more help:
#    - EMAIL_SETUP_GUIDE.md - Email configuration (Gmail, SendGrid, etc.)
#    - RENDER_DEPLOYMENT_GUIDE.md - Complete Render deployment instructions
